#include "main.h"
#include <stdio.h>

/**
 * main - print the number od argument
 * @argc: number of argument count
 * @argv: array of argument
 *
 * Return: Always 0.(sucess)
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
