#include "main.h"

/**
 * _abs - function that computes absolutes value
 * @b: inter to be checked
 *
 * Return: The absolute balue of integer
 */
int _abs(int b)
{
	if (b >= 0)
		return (b);
	else
		return (-b);
}
